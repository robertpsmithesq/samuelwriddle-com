---
interface Props {
  class?: string;
  interactive?: boolean;
}

const { class: className, interactive = false } = Astro.props;
---

<div
  class:list={[
    "card rounded-xl p-6",
    { "card--interactive": interactive },
    className,
  ]}
>
  <slot />
</div>
